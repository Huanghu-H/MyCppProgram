#include <iostream>
#include <vector>
using namespace std;

struct Node 
{
    int value;
    struct Node *next;
};

struct LinkedList 
{
    Node *head, *tail;
};

// 只需要完成此函数
// 可以定义其它函数作为这个函数的子过程，但是不需要自行定义main函数
// 不可以另外定义数组/STL容器
void insertSorted(LinkedList &list, vector<int> &vec) 
{
  	
}
int main()
{
	
}

